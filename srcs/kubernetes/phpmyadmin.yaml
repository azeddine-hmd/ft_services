apiVersion: v1
kind: Pod
metadata:
    name: phpmyadmin
spec:
    containers:
    - image: phpmyadmin
      imagePullPolicy: Never
      name: phpmyadmin
      livenessProbe:
          httpGet:
              path: /
              port: 8080
      ports:
      - containerPort: 5000
        protocol: TCP
